(()=>{'use strict';const key='city:'+document.body.dataset.city+':saved';let saved=[];try{const stored=JSON.parse(localStorage.getItem(key)||'[]');if(Array.isArray(stored))saved=stored.filter(Number.isInteger).slice(0,50);}catch{}let toastTimer;function toast(text){const el=document.querySelector('.toast');el.textContent=text;el.hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.hidden=true,3500);}function link(){return location.origin+'/explore?saved='+saved.join(',');}function render(){document.querySelectorAll('.saved-count').forEach(el=>el.textContent=saved.length);document.querySelectorAll('[data-save]').forEach(el=>{const yes=saved.includes(Number(el.dataset.save));el.setAttribute('aria-pressed',String(yes));if(el.classList.contains('save-button'))el.textContent=yes?'♥':'♡';else el.textContent=yes?'Saved to your list ♥':'Save this place ♡';});const empty=document.querySelector('#saved-empty');if(empty){empty.hidden=saved.length>0;document.querySelector('#saved-actions').hidden=!saved.length;document.querySelector('#saved-total').textContent=saved.length;document.querySelector('#saved-view').href=link();}}function persist(){try{localStorage.setItem(key,JSON.stringify(saved));return true;}catch{toast('This browser cannot save places. Please allow local storage.');return false;}}document.querySelectorAll('[data-save]').forEach(el=>el.addEventListener('click',()=>{const id=Number(el.dataset.save);const old=[...saved];if(saved.includes(id)){saved=saved.filter(x=>x!==id);}else{if(saved.length>=50){toast('Your list holds up to 50 places. Remove one to add another.');return;}saved.push(id);}if(!persist()){saved=old;return;}render();toast(saved.includes(id)?'Added to your saved places':'Removed from your saved places');}));document.querySelector('#saved-clear')?.addEventListener('click',()=>{const old=saved;saved=[];if(!persist()){saved=old;return;}render();toast('Your saved list is clear');});document.querySelector('#saved-share')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(link());toast('Shortlist link copied. Send it to your people.');}catch{toast('Open your shortlist and copy its address to share it.');}});window.addEventListener('storage',e=>{if(e.key===key){try{const next=JSON.parse(e.newValue||'[]');saved=Array.isArray(next)?next.filter(Number.isInteger).slice(0,50):[];render();}catch{}}});render();})();
