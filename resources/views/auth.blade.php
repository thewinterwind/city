@extends('layout')
@section('title',$register?'Create your free account':'Sign in')
@section('content')<section class="container section"><div class="form-card narrow"><span class="eyebrow">HELLO, LOCAL BUSINESS OWNERS</span><h1>{{ $register?'Let’s get you listed.':'Welcome back.' }}</h1><p>{{ $register?'Create a free account to add or claim your business.':'Sign in to manage your listings and requests.' }}</p><form method="post" action="{{ $register?'/register':'/login' }}">@csrf @if($register)<label>Your name<input name="name" value="{{ old('name') }}" required maxlength="100" autocomplete="name"></label>@endif<label>Email<input type="email" name="email" value="{{ old('email') }}" required maxlength="254" autocomplete="email"></label><label>Password<input type="password" name="password" required autocomplete="{{ $register?'new-password':'current-password' }}" @if($register) minlength="12" @endif></label>@if($register)<p class="small muted">Use at least 12 characters.</p><label>Confirm password<input type="password" name="password_confirmation" required minlength="12" autocomplete="new-password"></label><label class="check"><input type="checkbox" name="accept" value="1" required>I agree to the <a href="/privacy">privacy and site information</a>.</label><div class="honeypot" aria-hidden="true"><label>Company website<input name="company_website" tabindex="-1" autocomplete="off"></label></div>@endif<button class="button" type="submit">{{ $register?'Create free account':'Sign in' }} ↗</button></form><p>{{ $register?'Already have an account?':'New here?' }} <a href="{{ $register?'/login':'/register' }}">{{ $register?'Sign in':'Create a free account' }}</a></p></div></section>@endsection
