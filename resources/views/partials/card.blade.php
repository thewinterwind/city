<article class="place-card"><div class="card-image"><a href="/places/{{ $listing->slug }}" tabindex="-1" aria-hidden="true"><img loading="lazy" src="{{ $listing->image }}" alt="{{ $listing->photo_path?$listing->name:'' }}" width="420" height="280"></a>@unless($listing->photo_path)<span class="illustration-label">Category illustration</span>@endunless<button type="button" class="save-button" data-save="{{ $listing->id }}" aria-label="Save {{ $listing->name }}" aria-pressed="false">♡</button></div><div class="card-body"><span class="category-label">{{ $listing->category->name }}</span><h3><a href="/places/{{ $listing->slug }}">{{ $listing->name }}</a></h3><p class="card-description">{{ Str::limit($listing->description,115) }}</p><div class="card-bottom"><span>⌖ {{ $listing->area }}</span><a href="/places/{{ $listing->slug }}" aria-label="View {{ $listing->name }}">↗</a></div></div></article>
